import { Platform } from 'react-native'
const android = Platform.OS === 'android' ? true : false
export const SF_UI_Display_Bold = android ? 'SF-UI-Display_Bold' : 'VINCHAND'
export const SF_UI_Display_Regular = android ? 'SF-UI-Display_Regular' : 'VINCHAND'
